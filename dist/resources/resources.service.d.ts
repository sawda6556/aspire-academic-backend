import { Repository, DataSource } from 'typeorm';
import { Resource } from './entities/resource.entity';
import { Category } from './entities/category.entity';
import { ResourcePurchase } from './entities/resource-purchase.entity';
import { ResourceReview } from './entities/resource-review.entity';
import { ResourceStatus } from '../common/enums';
import { CreateResourceDto } from './dto/create-resource.dto';
import { UpdateResourceDto } from './dto/update-resource.dto';
export declare class ResourcesService {
    private readonly resourceRepository;
    private readonly categoryRepository;
    private readonly purchaseRepository;
    private readonly reviewRepository;
    private readonly dataSource;
    constructor(resourceRepository: Repository<Resource>, categoryRepository: Repository<Category>, purchaseRepository: Repository<ResourcePurchase>, reviewRepository: Repository<ResourceReview>, dataSource: DataSource);
    findAll(filters: any): Promise<{
        items: Resource[];
        total: number;
        page: any;
        limit: any;
    }>;
    findOne(id: string): Promise<Resource>;
    create(tutorId: string, dto: CreateResourceDto): Promise<Resource>;
    update(id: string, tutorId: string, dto: UpdateResourceDto): Promise<Resource>;
    purchase(userId: string, resourceId: string, paymentMethodId: string): Promise<ResourcePurchase>;
    findPurchased(userId: string): Promise<{
        download_url: string;
        purchased_at: Date;
        id: string;
        tutor_id: string;
        title: string;
        description: string;
        price: number;
        file_url: string;
        preview_url: string;
        category_id: string;
        subjects: string[];
        grade_level: string;
        status: ResourceStatus;
        average_rating: number;
        review_count: number;
        created_at: Date;
        updated_at: Date;
        tutor: import("../tutor-profiles/entities/tutor-profile.entity").TutorProfile;
        category: Category;
        reviews: ResourceReview[];
    }[]>;
    addReview(userId: string, resourceId: string, rating: number, comment: string): Promise<ResourceReview>;
    findTutorResources(tutorId: string): Promise<Resource[]>;
    findAllCategories(): Promise<Category[]>;
}
